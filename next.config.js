// Next.js configuration
// Allow loading images from unsplash domains if you choose to use remote
/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com'],
  },
};

module.exports = nextConfig;